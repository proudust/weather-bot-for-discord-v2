env:
  es6: true

extends:
  - prettier

parserOptions:
  sourceType: module
  ecmaVersion: 2018

overrides:
  - files: ["**/*.ts"]
    parser: "@typescript-eslint/parser"
    extends:
      - plugin:@typescript-eslint/recommended
    plugins:
      - "@typescript-eslint"
